
import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF071326);
  static const Color card = Color(0xFF101F37);

  static const Color primary = Color(0xFF7C3AED);
  static const Color secondary = Color(0xFF2563EB);
  static const Color accent = Color(0xFF9B7CFF);

  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white60;

  static const Color border = Colors.white12;
}